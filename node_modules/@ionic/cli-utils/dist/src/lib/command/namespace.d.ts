import { ICommand, ICommandMap, INamespace, INamespaceMap } from '../../definitions';
export declare class CommandMap extends Map<string, ICommand> {
}
export declare class NamespaceMap extends Map<string, INamespace> {
}
export declare class Namespace implements INamespace {
    getNamespaces(): INamespaceMap;
    getCommands(): ICommandMap;
    /**
     * Recursively inspect inputs supplied to walk down all the tree of namespaces
     * available to find the command that we will execute.
     */
    locateCommand(argv: string[]): [string[], ICommand | undefined];
}
/**
 * Get all commands for a namespace. Return a flat structure
 */
export declare function getCommandMetadataList(namespace: INamespace, namespaceDepthList?: string[]): [any];
